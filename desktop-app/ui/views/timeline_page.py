from ui.widgets.timeline_view import TimelineView

class TimelinePage(TimelineView):
    """
    Wrapper around TimelineView to treat it as a page.
    Currently just inheriting, but allows for page-specific logic later.
    """
    pass
